# SpecScore Repo Config Schema: https://specscore.md/repo-config

project:
    title: yardius-contract
    host: github.com
    org: sneat-co
    repo: yardius-contract
publication:
    default:
        actions:
            - stage
            - commit
            - push
    push:
        allow_branches:
            - main
        deny_branches: []
