# SpecScore Repo Config Schema: https://specscore.md/repo-config

project:
    title: ext-yardius
    host: github.com
    org: sneat-co
    repo: ext-yardius
publication:
    default:
        actions:
            - stage
            - commit
            - push
    push:
        allow_branches:
            - main
        deny_branches: []
